node_modules/
dist/
coverage/
test-report/
*.tsbuildinfo
*.tgz

# what the harness saw
.harness/
# vitest browser mode saves a screenshot next to a failing test
__screenshots__/
# a recorded device baseline is meant to be committed
!device/__screenshots__/
!device/__screenshots__/**

.DS_Store
npm-debug.*
.env*.local

# Claude Code: the shared configuration is committed, the personal parts are not
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/agent-memory-local/
.claude/worktrees/
